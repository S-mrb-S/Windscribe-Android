query GetLoginQuery($key: String!) {
  service(key: $key) {
      username
      password
      dailyQuota
      isActive
      isTrial
      uuid
      name
      days
      daysLeft
      quotaSum
      quotaLeft
      dailyQuotaLeft
  }
}
