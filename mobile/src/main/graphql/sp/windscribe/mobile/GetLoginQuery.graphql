query GetLoginQuery($key: String!) {
  service(key: $key) {
      dailyQuota
      isActive
      isTrial
      uuid
      name
      days
      daysLeft
      quotaSum
      quotaLeft
      dailyQuotaLeft
  }
}
